Тип `Cubux.AccountGroup`
========================

Объект со следующими полями:

Поле | Тип | Описание
---- | --- | --------
**`uuid`** PK | `uuid`   | UUID
`name`        | `string` | **Обязательно**. Название
`sort`        | `int`    | Порядок сортировки. От `0` до `32767`.
